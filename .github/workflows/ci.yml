name: 'Hello Action'
description: 'This action prints a greeting message.'
inputs:
  name:
    description: 'Name to greet'
    required: true
runs:
  using: 'composite'
  shell: bash   # Specify the shell environment as bash
  steps:
    - run: |
        echo "Hello ${{ inputs.name }}!"
